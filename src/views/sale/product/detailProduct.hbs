    <h1 class="page-header border-bottom pb-2">{{product.title}}</h1>
<div class="row">

    <div class="col-xs-12 col-md-5">
        <img class="spi" src="/uploads/{{product._id}}/{{product.image}}" alt=" "><br>
    </div>
    <div class="col-xs-12 col-md-7">
        <p>Desciption: {{product.desc}}</p>
        <p>Category: {{ctg}}</p>
        <p>Price: ${{Price}}</p>
        <p><a href="/cart/add{{product.slug}}">Add to cart</a></p>
    </div>

    <div class="col-xs-12">
        <ul class="gallery">
            {{{detailProduct gallery_image product}}}
        </ul>
    </div>
</div>
